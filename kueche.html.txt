<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Küche</title>
<link rel="icon" href="favicon.png">

<style>
body{
  font-family:Arial;
  background:#222;
  color:white;
  text-align:center;
  margin:0;
  padding:0;
}

h1{
  margin-top:20px;
  font-size:36px;
  color:#d35400;
}

.order{
  background:#333;
  margin:20px auto;
  padding:25px;
  font-size:28px;
  border-radius:15px;
  width:90%;
  max-width:600px;
  text-align:left;
  box-shadow:0 0 10px rgba(0,0,0,0.5);
}

.order p{
  margin:5px 0;
  font-size:24px;
}

button{
  font-size:24px;
  padding:15px 25px;
  margin-top:15px;
  border:none;
  border-radius:10px;
  background:#d35400;
  color:white;
  cursor:pointer;
  display:block;
}

button:hover{
  background:#e67e22;
}
</style>
</head>
<body>

<h1>👨‍🍳 Küche</h1>
<div id="orders"></div>

<script>
function loadOrders(){
  let orders = JSON.parse(localStorage.getItem("orders")) || []
  let container = document.getElementById("orders")
  container.innerHTML=""

  orders.forEach(order=>{
    let div = document.createElement("div")
    div.className="order"

    let extrasText = ""
    if(order.extras.length>0) extrasText = "Extras: "+order.extras.join(", ")

    div.innerHTML = `
      <p><strong>${order.number} - ${order.item}</strong></p>
      ${extrasText ? `<p>${extrasText}</p>` : ''}
      <button onclick="finish(${order.id})">Fertig</button>
    `
    container.appendChild(div)
  })
}

function finish(id){
  let orders = JSON.parse(localStorage.getItem("orders")) || []
  orders = orders.filter(o => o.id !== id)
  localStorage.setItem("orders", JSON.stringify(orders))
  loadOrders()
}

// automatisch aktualisieren
setInterval(loadOrders,1000)
</script>

</body>
</html>
